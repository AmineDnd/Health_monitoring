<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- ============================================================
             MAIL MESSAGE SECURITY
             Users can only edit/delete their OWN messages.
             Admins can moderate all messages.
             Read and Create are unrestricted (audit trail).
             ============================================================ -->
        <record id="rule_mail_message_own_write" model="ir.rule">
            <field name="name">Own Messages Only (Write/Delete)</field>
            <field name="model_id" ref="mail.model_mail_message"/>
            <field name="domain_force">[('author_id', '=', user.partner_id.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="perm_read" eval="False"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="True"/>
        </record>

        <record id="rule_mail_message_admin_full" model="ir.rule">
            <field name="name">Admin Full Message Access</field>
            <field name="model_id" ref="mail.model_mail_message"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('health_monitoring.group_health_admin'))]"/>
            <field name="perm_read" eval="False"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="True"/>
        </record>

    </data>
</odoo>
